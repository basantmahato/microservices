services:
  client:
    ports:
      - "${CLIENT_PORT:-5173}:5173"

  main-backend:
    ports:
      - "${MAIN_BACKEND_PORT:-3000}:3000"

  postgres:
    ports:
      - "${POSTGRES_PORT:-5432}:5432"

  mongo:
    ports:
      - "${MONGO_PORT:-27017}:27017"
